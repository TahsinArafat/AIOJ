DROP INDEX IF EXISTS idx_submissions_pending_poll;-- Reverting the enum value is intentionally unsupported; retain the final index.
-- The index is intentionally left in place because the enum value remains.
