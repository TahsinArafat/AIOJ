Next: Phase D core shipped (flags, problem discussion, achievements, follows). Next: Phase D remaining (activity feed, MOSS) or Phase E.
- **Audit fix #4 (mobile/tablet horizontal overflow): SHIPPED + verified** — ROOT CAUSE was **not** the sidebars: every routed page is a flex item of `<main class="... flex flex-col">` and almost every page root carries `mx-auto`. Auto cross-axis margins cancel `align-items: stretch`, so the page root fell back to **fit-content** sizing, which floors at the min-content width of its widest `whitespace-nowrap` table (609px on /contests) → document 691px at 390px. **`min-w-0` does NOT fix it** — fit-content is computed from min/max-content independently of `min-width`; only an explicit `width` does. Fix: `App.tsx` main gets `w-full min-w-0 [&>*]:w-full [&>*]:min-w-0`. Also: sidebars in AdminDashboard/ContestManage/SetterProblemWorkspace → `flex-col lg:flex-row` + horizontal tab scroller on mobile; ContestDetail tab strip + title badges get `overflow-x-auto`/`flex-wrap`; SetterPanel header buttons wrap; GroupList join row wraps; CookieConsent `max-w-[calc(100vw-2rem)]` (a `fixed` box with left+right and no max-width widened the document to 663px). **New bug found while sweeping: Navbar desktop cluster switched on at `md` (768px) but needs ~964px — on 768–964px screens the language selector, theme toggle and account menu were clipped off-screen. Moved both clusters to `lg`.** Verified: 44/44 routes docW==390 and not scrollable at 390px; 820px and 1920px clean; desktop nav intact. New gate `web/e2e/mobile-overflow.spec.ts` (15 tests, asserts real horizontal scroll via scrollTo, not just scrollWidth) — proven to fail 6/15 when the App.tsx fix is reverted.
- **Contest audit fixes #1–#3: SHIPPED + verified live** — (1) `NewRatingHandler(rs, contestStore)`; `GetByContest` resolves display_id/slug→UUID, 404s unknown contests; scoreboard `/api/rating/contest/12` was 500, now 200. (2) Removed duplicate `NotificationBell` from Navbar's mobile cluster; added single-flight `refreshAccessToken()` in `api.ts`; labelled bell + account dropdown. (3) Added `hashchange` listeners + `history.replaceState` to AdminDashboard / ContestManage / SetterProblemWorkspace. Verified: go build/vet/test, tsc, vitest 69/69, 1 refresh on concurrent 401, hash deep links work.
- **Phase E ops maturity: DONE** — migration 000065 audit_log; AuditRecorder middleware; HealthChecker; replica DSN; DR runbook; SLA alerts.
- **Phase D remainder (activity + MOSS): DONE** — GET /api/activity; ActivityFeed on Home; MossClient optional.
- **Phase D remainder 2: DONE** — editorialVisible() gate (migrate 66); aioj-cli; docs/openapi; GET /status.
- **CI failure emails: FIXED (main green)** — katex peer pin; openTestDB skip; golangci v2 goinstall; .golangci.yml debt exclusions; deploy soft-skip.
- **Auth settings / OAuth / 2FA UI (A.32): DONE** — api.twoFactor; TwoFactorSetup/Verify; Settings hub; i18n en/bn.
- **Phase B CI/CD: DONE** — backend-test/frontend-build/lint/docker-image/deploy-staging; dependabot; Makefile targets.
- **Phase C core (i18n/CDN): DONE** — locales zh/ru/ja/es; isRtl; Caddy cache headers; CDN purge; runbooks.
- **Phase D core: DONE** — CountryFlag; CommentSection; achievements/friendships migrations; follow API.
- **IDE UI layout fix (uncommitted working tree)** — `/ide` uses `h-[100dvh] overflow-hidden`, no site footer; CodeEditor fills `h-full`; global.css height 100%; Monaco `c-*` maps to `c`.
- **Judge DNS / cgroup crash-loop: FIXED** — `cgroup: host` on judge service in base `docker-compose.yml`.
- **Scope: interactive NOT required for run path** — CustomRun/IDE always uses pregiven stdin.
- **Frontend audit (general, browser-simulated)** — remaining: rankings API returns `{data:[],total:0}` while stats say 34 users; Admin Users Actions column empty (`{/* Actions */}`); 10px AC/date badges; blue gradient hero + Practice "Level Up" chrome; repetitive activity feed; ~20px nav hit targets; hard-coded EN in home sidebar; no prefers-reduced-motion rules; unlabeled icon buttons + bare `img` in nav.
- **Contest/management audit — still open** — (e) ContestManage shows raw `problem_id` UUID, ▲▼ 19px hit area; (f) scoreboard "Team" header for individual ACM contests, 10px problem `<th>`; (g) Gym/Plagiarism/Standings/Notifications pages are bare "No X" text; (h) Booklet/Prints/Statistics/Plagiarisms tabs are a single link or empty; duplicate contest-settings screens (`/setter/contest/12/manage#settings` vs `/setter/contest/12/edit`); `confirm()`/`alert()` in admin + manage.
