- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. Push approved when asked to proceed (this session: "verify and commit" + "do the fixes" → pushed). Current: main == origin/main at `2af41ba`. Working tree = their 12-file WIP only (uncommitted, last touched 18:58, session dormant since).
- **ESLINT DEBT IS DONE (except 3)**: first sweep (`92f0148` + `b04e218`) cleaned 55 files; second sweep (`2af41ba`, this session) took the remaining tree 126 → **3**. The 3 live in their concurrent uncommitted files (ContestList:246 any, Profile:203 + BotAccountsPanel:91 set-state-in-effect) — deliberately untouched because another session/user was actively editing them. If those files go idle again, fix + commit them for eslint 0. Whole-tree HEAD baseline still shows ~212 pre-existing problems in THEIR files at HEAD (their WIP fixes most).
- **HEAD-worktree verification is now standard procedure** for commits that ride alongside another session's uncommitted typing changes: `worktree_create` → symlink `node_modules` → copy ONLY your changed files → `npx tsc -p tsconfig.app.json --noEmit` + `npx vitest run` there. It caught 4 real breaks (dropped `.map((x))` annotations become implicit-any when upstream returns `any` at HEAD) that local gates with their dirty api.ts could never see. Remove with `worktree_remove force=true`. Note: `.sleepycode/worktrees/` may show untracked — never commit it.
- **Their parallel work in flux**: `web/src/types/api.ts` is UNTRACKED at HEAD (does not exist in commits); their dirty `lib/api.ts` is fully typed (imports all models from types/api.ts) while HEAD's api.ts has 200 `request<T>` generics but key methods still return `request<any>` (getProblemByIndex → `{problem: any; contest: any}`). **Page edits must NOT import from '../types/api'** — use page-local interfaces (fields from render/fetch usage), or derive: `type LoadedX = Awaited<ReturnType<typeof api.a.b>>['...']` (degrades gracefully to any at HEAD). Catch pattern: `catch (e) { errorMessage(e) }` via `src/lib/errors.ts` (exists at HEAD).
- **Patterns proven in sweep 2**: queueMicrotask(...) to defer sync setState in effects (order-safe: microtask beats fetch .then); JWT role decode → `useState(() => ...)` lazy init (kills set-state + no-empty together); loading seed from token/user presence (`useState(() => !getAccessToken())`); inline render component → rename to lowercase fn + call `{filterSidebar()}` (fixes remount/focus-loss, passes static-components); exhaustive-deps → sanctioned line-disable where deps would alter trigger sets; spread-of-possibly-null updater → null-guard like sibling branch; `as string`/`?? ''` only where runtime must stay identical.
- **delegate_task workers: 5/5 FAILED** (3 earlier + this session's ContestManage attempt = empty no-op). NEVER delegate implementation — do dense multi-file sweeps yourself with atomic count-asserted python heredocs (per-file atomic: MISS skips that file, others still apply).
- Shipped history (all pushed): `13a8761` reorder UX; `ad47390` EmptyState ×11; `a604f5d` lazy routes; perf split (chunks <500 kB); `d047ad4` stuck-loading; `022f4bf`+`19b5d79` audit; eslint sweeps `774b7d9`/`378b89f`/`25fba1e`/`92f0148`; `b85646a` memory; `b04e218` community typification + 4 tsc baseline fixes (Docker build unblocked: tsc -b, npm run build, go test all green); `19c751e` memory; `2af41ba` eslint sweep 2. Gates today: local tsc 0 / vitest 95/95 / build green / go 0 FAIL / eslint 3 (held); HEAD worktree tsc 0 / vitest 95 / my-files eslint 0.
- Spot-check recipe: `terminal_start name=vite-dev` → in session `cd web && npx vite --port 5199 --strictPort` (new terminal sessions fail `cd /abs` — use relative). Vite proxies /api→8080; login ai / Aioj-Sim-Admin-2026!. Data: only seeded problem `hello`; test contest display_id 18. Long-lived servers in terminal sessions; run_command timeout ≤120s → nohup+poll for docker builds. Older notes: leaderboard fix via registration-time user_profiles + migration 71; mobile overflow fixed in App.tsx shells; EmptyState API `{ icon?, text, description? }`.
