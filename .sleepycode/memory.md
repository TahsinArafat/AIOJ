- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. Push approved each session when asked. Current: main == origin/main at `d047ad4`.
- A SECOND parallel SleepyCode session shares this workspace: authored `ad47390` (shared EmptyState + nav.login i18n fix), edited App.tsx lazy routes, owns persistent terminal `fe-rebuild`. Check `git status` + file mtimes before editing; `replace_text` calls to the SAME file in one parallel batch get rejected — run them sequentially.
- Shipped commits (all pushed, all verified): `13a8761` contest reorder feedback + empty states; `ad47390` shared `web/src/components/EmptyState.tsx` across 11 pages + `common.login`→`nav.login`; `a604f5d` lazy-ify ~60 App.tsx routes + Suspense (`common.loading`) — entry 2015 kB→389 kB; `perf` statement-editor split: StatementTab lazy-loads VisualEditor + rolldown `build.rolldownOptions.output.codeSplitting.groups` (editor-tiptap 394 / editor-highlight 162 / editor-turndown 11 kB) — setter workspace chunk 615→50 kB, **no chunk >500, build warning gone**; `d047ad4` fix: SetterProblemWorkspace used to hang on "Loading problem workspace..." when api.problems.get failed (gated only on `if (!problem)`), now shows the error + Retry (regression test in SetterProblemWorkspace.test.tsx).
- i18n guard: `web/src/i18n/i18n-keys.test.ts` scans every literal t('…') via import.meta.glob against en.json — missing literal keys fail tests. Frontend suite: 92 tests green (`npm test -- --run`).
- Deployment: frontend has NO bind mounts — bundle baked into image (`docker compose build frontend && docker compose up -d frontend`, from ./web). A shell `cd /Users/tahsinarrafat/App_Dev/AIOJ` can transiently fail ("No such file") in NEW terminal sessions while the existing run_command session keeps working — drive Docker from run_command with `nohup ... & /tmp/*.log` polling (timeoutSeconds max 120). Verify deploy: `curl -s localhost/ | grep -o 'assets/[^"]*\.js'` + `docker exec aioj-frontend-1 sh -c 'ls -la /usr/share/nginx/html/assets/ | sort -k5 -rn | head'`. Missing assets get nginx SPA-fallback 200 with index.html (check Content-Length). Lazy-route check: browser `performance.getEntriesByType('resource')` lists only index + needed chunks.
- Seeded admin login: `ai` / `Aioj-Sim-Admin-2026!` (type into input[name=username]/[name=password], verify values before Enter — React controlled inputs dropped a keystroke once). Port 5173 = unrelated JinnChat app (its Vue-router/AuthApi 404s pollute console/network lists — filter to localhost/api and ignore stale entries across navigations). AIOJ web = port 80, backend 8080.
- Data state: `ai-sum-two-numbers` and other seeded sim problems were DELETED between sessions — only `hello` remains (`/api/problems` total=1). The setter spot-check uses `/setter/hello`; nonexistent slugs now show the retry screen ("not found" + Retry) instead of hanging.
- Older durable notes: leaderboard empty-list bug fixed via registration-time user_profiles creation, LEFT JOIN, migration 71; notification single-flight refresh and hash deep-link fixes shipped; mobile overflow root cause is fit-content sizing from auto cross-axis margins (fixed in App.tsx responsive shells); ContestManage reorder UX (40x40 buttons, aria-busy lock, optimistic swap on Promise.allSettled success, toasts, ChallengesTab exported for tests).
- Remaining candidates (reproduce before claiming): Gym/Plagiarism/Standings/Booklet/Statistics bare empty states outside contest manage; scoreboard "Team" header; raw i18n keys on pages reachable only with data (literal keys now guarded by test); eslint ~600 pre-existing `any` errors (non-gating).
