- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. Push approved each session when asked to proceed; this session user said "verify and commit" and push went through with it. Current: main == origin/main at `b04e218`, **working tree 100% clean (0 dirty, 0 untracked)** for the first time in days.
- **Docker image deploy is UNBLOCKED**: the 4 long-standing tsc baseline errors (ClassDetail 99, OrganizationDetail 180, TrainingPlanDetail 78×2) are FIXED at `b04e218` — `tsc -b` = 0 errors, `npm run build` green, vitest 95/95, `go build ./...` + `go test ./...` 0 failures. Next deploy: `docker compose build frontend` can now succeed (drive via run_command, timeout max 120s → nohup+poll; long-lived servers in terminal sessions).
- The former "parallel session" WIP (25 files, idle since 05:36) was committed wholesale at USER request in `b04e218`: their typification pass (`any` → local interfaces, new `web/src/types/community.ts` shared membership shapes, new `web/src/lib/errors.ts` `errorMessage()` used by ~8 pages for `catch { errorMessage(e) }`) + my 3 type fixes (optional `joined_at` → `Date(joined_at ?? '')` Invalid-Date parity; TrainingPlan enroll updater null-guarded `p` like the unenroll branch). No protected dirty files remain — but there may still be OTHER SleepyCode sessions; check `git status` + mtimes before editing, never parallel-edit the same file (edits race and roll back — redo sequentially).
- **Remaining debt**: `npx eslint .` = 335 problems (326 errors, 9 warnings), ALL inside the now-committed files from b04e218 (the sweep plan had scoped those files out). Not a build gate (`build = tsc -b && vite build`); a future cleanup pass can reuse the sweep patterns below.
- **Sweep patterns that worked (reuse for the 335)**: typed state via local interfaces (fields from `grep -oE '\bvar\.\w+'` + tsc iteration); catch → `e instanceof Error ? e.message : String(e)` (or their new `errorMessage(e)` helper — prefer it for consistency); sync setState in mount/tab effects → `queueMicrotask(...)` (pre-paint timing, passes react-hooks/set-state-in-effect) OR fold into fetch `.then` OR seed from useState initializer; render-phase Date.now → Date.parse/lazy or `// eslint-disable-line react-hooks/purity`; exhaustive-deps → sanctioned `// eslint-disable-line` (precedent CodeEditor/UserPublicProfile); co-located hooks (useToast/useConfirm/useTheme/flagEmoji) need react-refresh line-disables if moving them would churn imports; tiptap node views → `ReactNodeViewProps` from @tiptap/react; banners → useState(() => !localStorage.getItem(...)). Worker lesson: delegate_task workers failed 3/3 (early one-liners / no text) — do dense multi-file sweeps yourself via atomic python heredoc scripts with exact find/replace count assertions (a MISS leaves the file untouched).
- **Shipped history (pushed)**: `13a8761` reorder UX; `ad47390` EmptyState ×11; `a604f5d` lazy routes (entry 389 kB); perf split (setter chunk 50 kB, no chunk >500); `d047ad4` setter stuck-loading fix; `022f4bf`+`19b5d79` audit EmptyState polish + ProblemStats tests; eslint sweep `774b7d9`/`378b89f`/`25fba1e`/`92f0148` (55 clean files at 0 problems); `b85646a` memory; `b04e218` community typification + tsc fixes. Raw-i18n-keys candidate closed. eslint ~600 `any`s: 540→335 (remainder = b04e218 files).
- Spot-check recipe: `terminal_start name=vite-dev` → in session `cd web && npx vite --port 5199 --strictPort` (new terminal sessions fail `cd /abs` — use relative; run_command sees absolute paths fine). Vite proxies /api→8080; login ai / Aioj-Sim-Admin-2026!. EmptyStates verified on 5199 (gym 404, contest tabs, stats). Data: only seeded problem `hello` (42 subs); test contest display_id 18; backend/judge containers healthy. Older durable notes: leaderboard fix via registration-time user_profiles + migration 71; mobile overflow = fit-content sizing fixed in App.tsx shells; EmptyState API `{ icon?, text, description? }` (contest pages keep w-12 lucide icons).
