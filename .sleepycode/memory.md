Next: contest-management items (ChallengeTab UUIDs/reorder buttons, scoreboard "Team" header, duplicate contest-settings screens, confirm()/alert()), or the remaining general-audit polish.
- **Commit cadence: user asked for periodic git commits.** Done as 7 scoped commits on `main`: .gitignore, judge cgroup, rating display-id, IDE full-height, single-flight refresh + one bell, mobile/tablet overflow, docs memory, plus the rankings fix. Commits go straight to `main`; branch first if `main` is ever shared or protected.
- **audit/ and screenshots/ are now gitignored** — they are my browser-audit screenshots, not project assets. README's screenshot refs point at `/tmp/cf-screenshots/`, not the repo dir.
- **Empty leaderboard: FIXED + deployed** — `/api/rankings` returned `{data:[],total:0}` while `/api/stats` said 34 users. ROOT CAUSE: `UserStore.Create` only inserted into `users`; the `user_profiles` row was created **lazily by GetProfile** on first profile view. 32 of 34 users had no profile row and `ListUsersByRating`'s **INNER JOIN** dropped them all, so an empty leaderboard was indistinguishable from "nobody rated yet". Fix = 3 layers: (1) `Create` writes both rows in one tx; (2) `ListUsersByRating` LEFT JOIN in both count and page query — this also fixed the country/org filters, which referenced `up.*` and so dropped profile-less users too; (3) migration `000071_backfill_user_profiles` (down is an intentional no-op — the backfilled rows are indistinguishable from lazily created ones, so rolling back could delete real profiles). Verified: migration v71 clean, profiles 1→34, rankings total 32, home widget lists 10. **4 regression tests in `users_ranking_test.go`; 3 of 4 fail with the fix reverted.** Lesson worth reusing: "empty list" UI bugs are often an INNER JOIN against an optional/child table.
- **`openTestDB` pattern** (`internal/store/postgres/testing.go`): real Postgres, `t.Skip` when unreachable, DSN via `AIOJ_TEST_DSN` (defaults localhost:5432/aioj). Forcing a store-level failure: bake the target UUID into a `BEFORE INSERT` trigger body. **Dropping an FK does NOT make an insert fail** — I tried that first and the test failed to fail.
- **Audit fix #4 (mobile/tablet horizontal overflow): SHIPPED + verified** — ROOT CAUSE was **not** the sidebars: every routed page is a flex item of `<main class="... flex flex-col">` and almost every page root carries `mx-auto`. Auto cross-axis margins cancel `align-items: stretch`, so the page root fell back to **fit-content** sizing, which floors at the min-content width of its widest `whitespace-nowrap` table (609px on /contests) → document 691px at 390px. **`min-w-0` does NOT fix it** — fit-content is computed from min/max-content independently of `min-width`; only an explicit `width` does. Fix: `App.tsx` main gets `w-full min-w-0 [&>*]:w-full [&>*]:min-w-0`. Also: Navbar desktop cluster switched on at `md` (768px) but needs ~964px — on 768–964px the language selector, theme toggle and account menu were clipped; both clusters moved to `lg`. Sidebars in AdminDashboard/ContestManage/SetterProblemWorkspace → `flex-col lg:flex-row` + horizontal tab scroller on mobile; ContestDetail tab strip `overflow-x-auto` + title badges `flex-wrap`; SetterPanel header + GroupList join row wrap; CookieConsent `max-w-[calc(100vw-2rem)]` (a `fixed` box with left+right and no max-width widened the document to 663px). Verified 44/44 routes docW==390 and not scrollable at 390px; 820px and 1920px clean. New gate `web/e2e/mobile-overflow.spec.ts` (15 tests) asserts *real* scrollability via scrollTo+scrollX, not scrollWidth — `body{overflow-x:hidden}` makes scrollWidth over-report. Proven: 6/15 fail with the fix reverted.
- **Contest audit fixes #1–#3: SHIPPED + verified live** — (1) `NewRatingHandler(rs, contestStore)`; `GetByContest` resolves display_id/slug→UUID, 404s unknown contests; scoreboard `/api/rating/contest/12` was 500, now 200. (2) Removed duplicate `NotificationBell` from Navbar's mobile cluster; single-flight `refreshAccessToken()` in `api.ts`; labelled bell + account dropdown. (3) `hashchange` listeners + `history.replaceState` on AdminDashboard / ContestManage / SetterProblemWorkspace. go build/vet/test, tsc, vitest 69/69, 1 refresh on concurrent 401, hash deep links work.
- **Phase E ops maturity: DONE** — migration 000065 audit_log; AuditRecorder middleware; HealthChecker; replica DSN; DR runbook; SLA alerts.
- **Phase D remainder (activity + MOSS): DONE** — GET /api/activity; ActivityFeed on Home; MossClient optional.
- **Phase D remainder 2: DONE** — editorialVisible() gate (migrate 66); aioj-cli; docs/openapi; GET /status.
- **CI failure emails: FIXED (main green)** — katex peer pin; openTestDB skip; golangci v2 goinstall; .golangci.yml debt exclusions; deploy soft-skip.
- **Auth settings / OAuth / 2FA UI (A.32): DONE** — api.twoFactor; TwoFactorSetup/Verify; Settings hub; i18n en/bn.
- **Phase B CI/CD: DONE** — backend-test/frontend-build/lint/docker-image/deploy-staging; dependabot; Makefile targets.
- **Phase C core (i18n/CDN): DONE** — locales zh/ru/ja/es; isRtl; Caddy cache headers; CDN purge; runbooks.
- **Phase D core: DONE** — CountryFlag; CommentSection; achievements/friendships migrations; follow API.
- **IDE UI layout fix** — `/ide` uses `h-[100dvh] overflow-hidden`, no site footer; CodeEditor fills `h-full`; global.css height 100%; Monaco `c-*` maps to `c`.
- **Judge DNS / cgroup crash-loop: FIXED** — `cgroup: host` on judge service in base `docker-compose.yml`.
- **Scope: interactive NOT required for run path** — CustomRun/IDE always uses pregiven stdin.
- **Frontend audit (general, browser-simulated)** — remaining: Admin Users Actions column empty (`{/* Actions */}`); 10px AC/date badges; blue gradient hero + Practice "Level Up" chrome; repetitive activity feed; ~20px nav hit targets; hard-coded EN in home sidebar; no prefers-reduced-motion rules; bare `img` in nav.
- **Contest/management audit — still open** — (e) ContestManage shows raw `problem_id` UUID, ▲▼ 19px hit area; (f) scoreboard "Team" header for individual ACM contests, 10px problem `<th>`; (g) Gym/Plagiarism/Standings/Notifications pages are bare "No X" text; (h) Booklet/Prints/Statistics/Plagiarisms tabs are a single link or empty; duplicate contest-settings screens (`manage#settings` vs `/setter/contest/12/edit`); `confirm()`/`alert()` in admin + manage.
