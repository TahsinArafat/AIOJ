- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. Push approved each session when asked to proceed. Current: main == origin/main at `19b5d79`.
- A SECOND parallel SleepyCode session shares this workspace: authored `ad47390` (shared EmptyState + nav.login i18n), edited App.tsx lazy routes, owns terminal `fe-rebuild`, and has 25 dirty files UNCOMMITTED since ~05:36 (community feature: teams/orgs/groups/training plans + `web/src/lib/errors.ts`, `types/community.ts` new). Check `git status` + mtimes before editing; NEVER parallel-edit the same file (edits race and roll back — redo sequentially); never touch their dirty files.
- BLOCKER (as of 05:55): their WIP breaks `tsc -b` — errors in ClassDetail.tsx(99), OrganizationDetail.tsx(180), TrainingPlanDetail.tsx(78) — so `npm run build` / Docker frontend image build fails. The EmptyState polish (commits `022f4bf`, `19b5d79`) is pushed but NOT image-deployed; verify via local vite instead and let their session (or a later build once tsc is green) ship it. Vitest is tsc-free: 95 tests / 23 files green.
- Shipped commits (pushed): `13a8761` contest reorder feedback; `ad47390` EmptyState ×11 + i18n key; `a604f5d` lazy routes (entry 2015→389 kB); perf statement-editor split (setter chunk 615→50 kB, rolldown codeSplitting groups editor-tiptap/highlight/turndown, no chunk >500, warning gone); `d047ad4` setter workspace stuck-loading fix (error + Retry + test); `022f4bf` audit polish: ContestDetail ×6 + ProblemStats ×2 + GymDetail not-found → shared EmptyState; `19b5d79` ProblemStats.test (3 tests covering unreachable empty branches).
- Audit closure: raw-i18n-keys candidate CLOSED (no `t(\`dynamic\`` keys exist; i18n-keys.test green). Scoreboard "Team" header, GymList, ContestPlagiarism, Booklet (ContestManage) = parallel session's dirty files, skipped on purpose. ContestDetail "Team" hardcoded header is page-consistent English, not a raw key — left alone. eslint ~600 pre-existing `any`s deferred (would collide with their 25 files).
- Spot-check recipe for this turn: `terminal_start name=vite-dev` → in that session `cd web && npx vite --port 5199 --strictPort` (NEW terminal sessions fail `cd /abs/path` with "no such file" but relative `cd web` from the pre-set workspace cwd works; run_command sessions see absolute paths fine). Vite dev proxies /api→8080; login ai / Aioj-Sim-Admin-2026! works per-origin (cookies ignore ports). Verified on 5199: /gym/does-not-exist EmptyState, contest `2fe580e8-7588-4e39-aa9f-dc71b8dec48a` standings/submissions/clarifications/announcements empty states, /problems/hello Statistics tiles (42 subs, 69%).
- Deployment: frontend has NO bind mounts — image build = `docker compose build frontend && docker compose up -d frontend` (runs `tsc -b && vite build`, so blocked while their tsc is red). Drive docker from run_command; timeoutSeconds max 120 → use `nohup … > /tmp/log 2>&1 &` + poll. Verify: `curl -s localhost/ | grep -o 'assets/[^"]*\.js'`, `docker exec aioj-frontend-1 ls -la /usr/share/nginx/html/assets/`. Missing assets get nginx SPA-fallback 200 (check Content-Length). A `nohup &` child started in a run_command that TIMES OUT gets killed — use terminal_start sessions for long-lived servers.
- Data state: only seeded problem `hello` (42 subs); other sim problems deleted between sessions. Test contest exists (acm, display_id 18, "Managed title…"). Backend/judge containers up 8h+; postgres healthy.
- Older durable notes: leaderboard empty-list bug fixed via registration-time user_profiles creation, LEFT JOIN, migration 71; notification single-flight refresh and hash deep-link fixes shipped; mobile overflow root cause is fit-content sizing from auto cross-axis margins (fixed in App.tsx responsive shells); ContestManage reorder UX shipped; EmptyState API = `{ icon?, text, description? }`, icon slot text-4xl, convention mostly text+description, contest pages keep w-12 lucide icons inside.
