- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. Latest scoped commits: `22a667f feat(web): replace native feedback dialogs`, `361aaf1 refactor(web): consolidate contest settings`, `f98579b fix(api): normalize contest references`. main is ahead of origin/main by ~16 commits.
- Frontend audit fixes completed: shared ConfirmDialog/Toast providers and migration of native alert/confirm call sites; removed dead Admin Users Actions column; home i18n wiring; nav hit targets and reduced-motion CSS; mobile/tablet overflow fix (App.tsx explicit width/min-width and responsive management shells); legacy contest edit route now redirects to `/setter/contest/:id/manage#settings`; contest settings are canonical in ContestManage.
- Contest display-ID bug root cause: several routes accepted display_id/slug but passed it directly to UUID-keyed stores, causing `invalid input syntax for type uuid: "12"`. Shared `internal/api/handler/contest_ref.go` resolves references to canonical UUID; regression tests in `internal/api/handler/onsite_test.go` and `internal/hack/service_display_id_test.go`.
- Contest challenge reorder + empty-state consistency: SHIPPED in `web/src/pages/ContestManage.tsx`. Reorder = 40x40 buttons, per-problem accessible names (`Move <title> up/down`), global in-flight lock (reorderInFlight ref + aria-busy spinner), Promise.allSettled over per-row updateProblem calls, optimistic swap only on full success, error toast + `load()` reconciliation on failure, success toast. EmptyState gained optional `description` prop used by Challenges/Clarifications/Participants/Submissions/Prints/Balloons/Announcements/Moderators/Onsite Teams (Balloons distinguishes empty vs filtered-empty). `ChallengesTab` is exported for tests.
- Tests: new `web/src/pages/ContestManage.test.tsx` (5 tests: edge-disabled labels, successful swap + API payloads + toast, in-flight lock via shared gate promise, failure → alert + re-sync, empty-state copy). Full suite 20 files/87 tests green; `npm run build` green; eslint debt is pre-existing `any` types (602 errors repo-wide, none in the new test file).
- Deployment/verification: frontend image has no bind mounts — bundle is baked; rebuild with `docker compose build frontend && docker compose up -d frontend` (context ./web, Dockerfile). Seeded admin login for browser checks: `ai` / `Aioj-Sim-Admin-2026!` (SEED_ADMIN_* env, requires CSRF cookie so curl login fails with "csrf cookie missing"; log in via the web form). Browser-verified live: `/setter/contest/12/manage#challenges` renders reorder group with edge-disabled buttons, Announcements empty state shows title+description, mobile 390 has no horizontal overflow. Port 5173 is a DIFFERENT app (JinnChat/Vue) — AIOJ web is on port 80 (aioj-frontend nginx), backend 8080.
- Existing durable notes: leaderboard empty-list bug fixed via registration-time user_profiles creation, LEFT JOIN, migration 71; notification double-poll/single-flight refresh and hash deep-link fixes shipped; mobile overflow root cause is fit-content sizing from auto cross-axis margins, not merely min-w-0.
- Remaining lower-priority audit candidates: any other explicitly confirmed UX findings (Gym/Plagiarism/Standings bare empty states outside contest manage, Booklet/Statistics tabs, scoreboard "Team" header). Do not claim raw UUID/Team-header bugs without reproducing them.
