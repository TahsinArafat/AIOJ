Next: Phase D core shipped (flags, problem discussion, achievements, follows). Next: Phase D remaining (activity feed, MOSS) or Phase E.
- **Phase E ops maturity: DONE** — migration 000065 audit_log + store/list API; AuditRecorder middleware on /api/admin state-changing; HealthChecker (db/redis/queue depth, 503 only if db down); database.replica_dsn + DATABASE_REPLICA_DSN; DR runbook docs/runbooks/disaster-recovery.md; SLA alerts JudgeLatencyHigh + JudgeQueueBacklog in deploy/prometheus/alerts.yml; plan docs/superpowers/plans/2026-06-13-ops-maturity.md. Verified: go build, handler/middleware/config/postgres tests, migrate→65, vitest 58. Next: enable CF DNS + STAGING secrets; optional MOSS / status page / Crowdin.
- **Phase D remainder (activity + MOSS): DONE** — GET /api/activity merges AC/rating/comment; ActivityFeed on Home; MossClient (MOSS_USER/MOSS_URL env, optional); local LCS still default. Verified: go build + plagiarism/handler/postgres tests; vitest 60/60; tsc; vite build. Next: ops secrets (CF, staging); optional Crowdin/status/OpenAPI/CLI.
- **Cookie consent banner (A.31): DONE** — `CookieConsent` mounted in App.tsx; Accept/Decline store `cookie-consent` in localStorage; links Privacy Policy. Tests: show/accept/decline/already-decided (vitest CookieConsent 4/4). Verified: tsc, CookieConsent vitest. Next: Phase D core shipped (flags, problem discussion, achievements, follows). Next: Phase D remaining (activity feed, MOSS) or Phase E.
- **Auth settings / OAuth / 2FA UI (A.32): DONE** — `api.twoFactor` (begin/enable/disable/verify); pages `TwoFactorSetup`, `TwoFactorVerify`; Settings hub `/settings` (2FA, connected accounts, danger zone); Profile → Security Settings link; OAuth buttons + VerifyEmail already present. Routes: `/settings`, `/auth/2fa/setup`, `/auth/2fa/verify`. i18n auth keys en/bn (parity tests). Verified: tsc, vite build, vitest TwoFactor+locales+CookieConsent. Next: Phase B CI/CD plan.
- **Phase B CI/CD: DONE** — workflows: backend-test, frontend-build, lint (golangci + eslint continue-on-error for ~600 pre-existing debt), docker-image (GHCR), deploy-staging (needs STAGING_* secrets). dependabot gomod/npm/docker/actions. Makefile: test, test-frontend, lint, fmt, fmt-check, build-image. lefthook.yml optional. Plan: docs/superpowers/plans/2026-06-13-cicd-pipeline.md. Test fixes: matchMedia + elementFromPoint polyfills; ThemeContext default; ProblemDetail listI18n mock; StatementTab VisualEditor; workspace hash reset; local DB migrated 48→62. Verified: go test OK, vitest 48/48, gofmt, go vet, YAML parse. Next: Phase C (i18n locales, CDN/cache, multi-region runbook).
- **Phase C core (i18n/CDN): DONE** — locales zh/ru/ja/es (parity with en, 90 keys); registry isRtl + html dir sync; Caddy cache headers; features.remote_bots_regions + RemoteBotsAllowed; CDN purge GET status + POST /api/admin/cdn/purge (Cloudflare, CF_ZONE_ID/CF_API_TOKEN); runbooks multi-region + domain-policy; plan docs/superpowers/plans/2026-06-13-global-reach.md. Verified: vitest locales 13 + full 54; go handler/config; tsc; vite build. Next: Phase D (community) or remaining Phase C ops (Crowdin, status page).
- **Phase D core: DONE** — CountryFlag + rankings country; problem CommentSection; migrations 000063 achievements + 000064 friendships; AC awards first_ac/ten_ac/fifty_ac/hundred_ac; follow API (POST/DELETE/status/followers/following); api.users social methods. Verified: go build+tests, vitest 58, tsc, vite build, migrate→64. Next: remaining Phase D (activity feed UI, MOSS) or Phase E.
- **Phase D core (community): DONE** — CountryFlag + rankings country column; problem discussion CommentSection on ProblemDetail (parent_type already in DB CHECK); migrations 000063 achievements + 000064 friendships; award milestones in UpdateResult on AC; API: /users/{id}/achievements, follow/unfollow/status/followers/following. Verified: go build/test, vitest 58/58, tsc. Next: activity feed UI, MOSS (Phase D rest) or Phase E ops.
