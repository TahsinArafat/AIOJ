- User asked for periodic Git commits; work is committed directly to main unless they say otherwise. They approved pushing again this session. Current: main == origin/main at `a604f5d perf(web): lazy-load all route pages behind Suspense` (also `ad47390 feat(web): shared EmptyState component`, `13a8761 feat(web): improve contest challenge reorder feedback and empty states`).
- A SECOND parallel SleepyCode session shares this workspace: it authored `ad47390` (EmptyState/i18n), edited `web/src/App.tsx` (lazy routes), and owns a persistent terminal named `fe-rebuild`. Coordinate before editing App.tsx or rebuilding the frontend; check `git status` + file mtimes first.
- Contest challenge reorder + empty-state consistency SHIPPED: `web/src/pages/ContestManage.tsx` reorder = 40x40 buttons, per-problem `Move <title> up/down` labels, in-flight lock (reorderInFlight ref + aria-busy spinner), Promise.allSettled over updateProblem calls, optimistic swap only on full success, error toast + load() reconciliation, success toast. `ChallengesTab` exported for tests; `web/src/pages/ContestManage.test.tsx` (5 tests).
- Shared `web/src/components/EmptyState.tsx` (optional icon + description) replaces per-page copies; actionable copy added across all 11 former bare-state pages (BlogList, Rankings, Submissions, Notifications, GroupDetail, TeamDetail, OrganizationDetail, ProblemDetail, APISettings, TrainingPlanDetail, UserPublicProfile) and contest-manage tabs (Balloons distinguishes empty vs filtered-empty).
- i18n: `common.login/register` raw-key bug fixed → `nav.login/register` (Home in App.tsx). Regression net `web/src/i18n/i18n-keys.test.ts` scans every literal `t('…')` via import.meta.glob against the catalog — add keys to en.json or tests fail. Suspense fallback uses existing `common.loading`.
- Code-splitting SHIPPED: App.tsx now lazy()s ~60 pages and wraps Routes in Suspense. Entry chunk 2015 kB → 389 kB; dist has 87 JS chunks; biggest = SetterProblemWorkspace 615 kB (still trips the >500 kB build warning — candidate: split monaco/VisualEditor further via build.rolldownOptions.codeSplitting), katex 273 kB shared, lib 153 kB.
- Full frontend state: `npm test -- --run` 20+ files green (includes EmptyState.test.tsx, i18n-keys.test.ts, ContestManage.test.tsx); `npm run build` green (vite 8 rolldown). Repo-wide eslint still ~600 pre-existing `any` errors — not a gate.
- Deployment: frontend container has NO bind mounts — bundle baked into image; rebuild via `docker compose build frontend && docker compose up -d frontend` (context ./web). Verify deployment by `curl -s localhost/ | grep -o 'assets/[^"]*\.js'` (entry size) + `docker exec aioj-frontend-1 ls /usr/share/nginx/html/assets/`. Missing chunk names get nginx SPA-fallback 200 with index.html (1.5 kB) — check Content-Length, not status. Lazy-route check: browser performance.getEntriesByType('resource') should list only index + route chunk. Seeded admin login: `ai` / `Aioj-Sim-Admin-2026!` (web form; curl fails with 'csrf cookie missing'). Port 5173 is a DIFFERENT app (JinnChat/Vue); AIOJ web = port 80, backend 8080.
- Browser-session gotchas: browser_type into `input[type=text]` hit the navbar search first (no name attr) and React controlled inputs dropped a keystroke — type into `input[name=username]` / `input[name=password]` and verify values before submitting. Console/network arrays persist stale entries from previous pages in the session.
- Older durable notes: leaderboard empty-list bug fixed via registration-time user_profiles creation, LEFT JOIN, migration 71; notification single-flight refresh and hash deep-link fixes shipped; mobile overflow root cause is fit-content sizing from auto cross-axis margins (fixed in App.tsx responsive shells).
- Remaining candidates (reproduce before claiming): >500 kB SetterProblemWorkspace chunk; Gym/Plagiarism/Standings/Booklet/Statistics bare empty states outside contest manage; scoreboard "Team" header; raw i18n keys on other pages (i18n-keys test now guards literal keys).
